import { styled } from "../..";

export const HeaderContainer = styled('header', {
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
  padding: '2rem 8.5rem',
  margin: '0 auto',
  width: '100%',
  maxWidth: '100%',

  button: {
    marginLeft: 'auto',
  }
}
)